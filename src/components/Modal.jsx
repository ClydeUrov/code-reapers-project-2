import CustomButton from "./CustomButton";

const Modal = ({ setShowModal, showModal, title}) => {
  return (
    <div>
      <h2>{title}</h2>
      <form>
        <input />
        <input />
        <button />
      </form>
    </div>
  )
}

export default Modal;